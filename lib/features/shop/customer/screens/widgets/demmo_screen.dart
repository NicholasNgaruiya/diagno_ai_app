import 'package:flutter/material.dart';

class DemmoWidget extends StatelessWidget {
  const DemmoWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Products Page')),
    );
  }
}
