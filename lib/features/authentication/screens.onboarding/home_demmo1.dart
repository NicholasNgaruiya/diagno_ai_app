import 'package:flutter/material.dart';

class HomeDemmo1 extends StatelessWidget {
  const HomeDemmo1({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Home Demmo 1'),
      ),
    );
  }
}
